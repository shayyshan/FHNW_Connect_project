# API Configuration
PROJECT_NAME="FHNW Connect API"
VERSION="0.1.0"
API_PREFIX="/api"

# Database Configuration (PostgreSQL)
# Format: postgresql://user:password@server:port/database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fhnw_connect"

# CORS Setup (Generic)
# Comma-separated list of allowed origins (e.g., * for all, or specific domains)
CORS_ORIGINS="*"
